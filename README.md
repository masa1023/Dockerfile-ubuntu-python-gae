# Dockerfile-ubuntu-python-gae

## Install Python and GAE in Ubuntu

### build

`docker build --build-arg user_name="NEW_USER_NAME" --build-arg user_password="NEW_USER_PASSWORD" -t ubuntu-python-gae:latest .`
